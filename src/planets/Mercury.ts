import {createPlanet} from "../utils/createPlanet";
import {textures} from "../textures";

export const Mercury = createPlanet({ position: { x: 20, y: 0, z: 10 },  size: 4, texture: textures.mercury });
