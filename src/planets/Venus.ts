import {createPlanet} from "../utils/createPlanet";
import {venusTexture} from "../textures";

export const Venus = createPlanet({ position: { x: 50, y: -2, z: 20}, size: 8, texture: venusTexture  })
