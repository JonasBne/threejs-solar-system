import {createPlanet} from "../utils/createPlanet";
import {textures} from "../textures";

export const Jupiter = createPlanet({ position: { x: 50, y: 25, z: -80 },  size: 14, texture: textures.jupiter})
