import {createPlanet} from "../utils/createPlanet";
import {jupiterTexture} from "../textures";

export const Jupiter = createPlanet({ position: { x: 0, y: 20, z: -500 },  size: 88, texture: jupiterTexture})
