import {createPlanet} from "../utils/createPlanet";
import {marsTexture} from "../textures";

export const Mars = createPlanet({ position: { x: 0, y: 20, z: -120 },  size: 4.5, texture: marsTexture})
